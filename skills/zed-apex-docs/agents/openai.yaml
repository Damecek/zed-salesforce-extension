version: 1
display_name: Zed Salesforce DX Docs
short_description: Research workflow for Zed + Salesforce DX language tooling (Apex LSP backend today) using Context7 and primary sources
default_prompt: Use the zed-apex-docs workflow to answer the question using Context7 first, then the listed authoritative web sources if needed, and cross-check against local repo references (extension launcher in `src/lib.rs`, smoke test in `scripts/`) when relevant. Treat Apex LSP as the current backend and frame conclusions in Salesforce DX extension scope. Provide exact conclusions and cite sources.
